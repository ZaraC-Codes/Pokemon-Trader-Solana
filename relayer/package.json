{
  "name": "pokeball-relayer",
  "version": "1.0.0",
  "description": "Cloudflare Workers relayer for gasless PokeBall throws",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "viem": "^2.5.0"
  }
}
